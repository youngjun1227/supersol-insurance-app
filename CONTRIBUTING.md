# 협업 규칙

> 처음 합류했다면 [docs/온보딩.md](docs/온보딩.md) 를 먼저 읽으세요. 이 문서는 규칙 원문입니다.

## 역할 분담 (3인 개발)

| 담당 | 범위 | 화면 | 브랜치 |
|---|---|---|---|
| **팀장** (`youngjun1227`) | 공용 표면 | 공용 컴포넌트 · 탭바 · 메인홈 · 스켈레톤 · 라우터 | `feat/common` `feat/home` |
| 팀장 | 아이디어 1 · 4 | S2-A · S2-D · S6-A / S3-C·D·E·F | `feat/s2-*` `feat/s3-*` |
| **팀원 A** (`Jongkwang131`) | 아이디어 2 | S1 보험 메인 3상태 · S1-7 · S1-13 시트 | `feat/s1-main` |
| **팀원 B** (`leechanyoung0710`) | 아이디어 3 | S4-A 팝업 · S5-A · S4-D · 청구완료 | `feat/claim-flow` |
| 팀원 B | 운영 | `/moderator` 진행자 화면 — **앱 완성 후 착수** | `feat/moderator` |

배정 기준은 **난이도**입니다 — S1은 한 라우트에 상태 3개가 얽혀 로직이 까다롭고,
청구 흐름은 화면이 서로 독립적이라 상대적으로 단순합니다.
디자인 시안 담당과 개발 담당은 일치하지 않습니다(시안은 팀 전체 합의라 배분 근거가 아닙니다).

**공용 표면(홈·탭바·라우터)은 팀장이 소유합니다.** 세 사람 화면의 공통 진입점이라
주인이 없으면 서로 조금씩 고치다 충돌합니다. 테스트 과제 4개가 전부 홈에서 시작하므로
홈이 흔들리면 네 과제의 출발점이 전부 갈립니다.

**공용 컴포넌트(`src/components/`)도 팀장이 만듭니다.** 필요한 게 없으면 각자 만들지 말고
요청하세요 — 각자 만들면 크기·비율이 갈립니다.

## 브랜치

```
짧은 토픽 브랜치 (feat/s1-main 등, 화면 단위 1~2일)  →  PR  →  팀장이 squash merge  →  main
```

develop·사람별 장수 브랜치는 두지 않습니다 — 같은 코드베이스라 충돌만 커집니다.

- **`main` 직접 push 금지 — 예외 없음(팀장 포함).**
  GitHub 브랜치 보호 + `.githooks/pre-push` 가 막습니다.
  ⚠️ 이 훅은 규칙을 문서로만 뒀다가 실제로 어겨져서(화면 5장이 main 에 직접 올라감) 추가한 것입니다.
- **force push 전면 금지 — 모든 브랜치, 예외 없음.**
  저장소 룰셋(`non_fast_forward`) + `.githooks/pre-push` 가 막습니다.
  되돌릴 게 있으면 `git revert` 로 새 커밋을 쌓습니다.
  ⚠️ 히스토리를 다시 쓰면 남의 커밋이 조용히 사라집니다. 팀장이 자기 브랜치에서 force push 를
  했다가 GitHub 워크플로 이벤트가 유실돼 CI 가 아예 안 도는 일도 겪었습니다.
- 머지 후 브랜치 자동 삭제 · **squash merge 만**.

## PR

**PR 은 팀장도 예외 없이 거칩니다** — figma-ref 비교 스크린샷 포함. 이게 시각 검수의 유일한 관문입니다.

- **승인**: 팀원은 팀장 승인 1개 필요 / **팀장은 승인 없이 머지합니다** —
  승인해줄 사람이 팀장뿐이라 자기 PR 을 자기가 승인하는 건 의미가 없습니다.
  (`enforce_admins: false` · PR 화면의 `Merge without waiting for requirements to be met`)
- **PR 은 화면 구현·공용 컴포넌트·목데이터/토큰 변경에 올립니다.**
  PR 의 목적은 충돌 방지와 figma-ref 시각 검수인데, 문서·CI 설정 변경은 둘 다 해당이 없어
  작게 묶어 함께 올립니다.
- **작업은 이슈 단위** — 화면 1개 = 이슈 1개. 이슈 본문에 Figma 노드·레퍼런스 PNG·완료 기준이 있습니다.
  PR 본문에 `Closes #N` 을 적으면 머지 시 이슈가 닫힙니다.

## 커밋

- **커밋 메시지에 `Co-Authored-By: Claude …` / `Generated with Claude Code` 를 넣지 않습니다.**
  `.claude/settings.json`(`includeCoAuthoredBy: false`) + `.githooks/commit-msg` 가 강제합니다.
  clone 후 `git config core.hooksPath .githooks` 를 한 번 실행해야 훅이 붙습니다.
- 커밋 메시지는 **한국어**로 씁니다.

## CI

```
토큰 lint → 문구 컴플라이언스 → 목데이터 정합성 → typecheck → build → 번들 크기
```

`.github/workflows/ci.yml`. pre-commit 훅을 우회해도 CI 가 막습니다.

## 코드 규칙

- **토큰 밖 색·라운드·폰트 크기 금지** — `src/styles/tokens.css` 가 전부입니다.
  토큰 밖 값을 발견하면 **가장 가까운 토큰으로 스냅해 구현하고, 변경로그에 한 줄 보고**합니다
  (Figma 는 팀장이 고칩니다).
- `font-size` 를 직접 쓰지 않습니다 — `src/styles/typography.css` 의 13단계 유틸 클래스(`.t-h1` 등).
- 계측 `targetId` 는 반드시 `src/lib/targetId.ts` 의 `tid()` 로 만듭니다.
- **화면을 추가하면 계측도 같이 붙입니다** — 나중에 소급하면 화면을 전부 다시 열어야 합니다.
- 데이터 접근은 `useMock()` 하나로 — 화면에서 목데이터 모듈을 직접 import 하지 않습니다.
- **임의 목데이터 생성 금지** — 원본은 디자인 레포 `02_to-be/mock-data.md` 입니다.
- 높이는 `100dvh` — **`100vh` 금지**(iOS Safari 하단 바).

## 문구 규칙

- UI·문서에 "개선안"·"솔루션"·"MVP"·팀명을 쓰지 않습니다.
- 회사 관점 문구 금지("노출 증대"·"가입 전환") — 사용자 관점으로만 씁니다.
- 어조는 슈퍼SOL "~해요"체.
- **실명·실번호·타사 실제 상품명 금지** — 김신한 / 010-0000-0000 / A생명·D생명.
- 한국 사회적 상징으로 읽히는 이미지를 쓰지 않습니다.

⚠️ **이 저장소는 public 입니다. 문서·코드·커밋 어디에도 실명을 쓰지 않습니다** — GitHub 계정명만 씁니다.

## 일정

React 1차 구현 8/25~31 → 9/1~ 다듬기·배포 → **9/10 main 프리즈**(핫픽스만) →
🔒 9/11 사용자 테스트 → 9/20 발표 → 이후 기능 확장.
