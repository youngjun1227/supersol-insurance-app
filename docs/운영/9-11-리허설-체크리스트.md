# 9/11 리허설 + 계측 로그 백업 절차

> 이슈 #83 에서 옮겨옴 (2026-08-29). 개발 작업이 아니라 운영 절차라 문서로 관리한다.
> **선행 조건: 평가지·taskId 경계 확정** (CLAUDE.md 보류 항목). 확정되는 대로 착수 —
> 9/10 프리즈 역산하면 **9/8 주간에 리허설 1회**가 마지노선이다.

## 1. 계측 로그 백업 절차

로그는 참가자 폰 브라우저의 **localStorage 한 곳**에만 쌓인다. 브라우저 데이터가
날아가면(시크릿 모드 종료·저장공간 정리) 그 세션 로그는 유실이다.

- **참가자 1명 끝날 때마다 `/export` 에서 내보내기** — 진행자 체크리스트에 명문화
- 코드 보완 후보(선택): 과제 종료 시 자동 다운로드 등은 `/moderator` 착수 때 함께

## 2. 운영 리허설 체크리스트 (아직 0회)

- [ ] Vercel 프로덕션 연결 (팀장 1회 작업 — 아직 미연결)
- [ ] 폰 브라우저에서 홈 화면 추가(PWA) → 전체화면 실행 확인
- [ ] 과제 4개를 처음부터 끝까지 실제 폰으로 1바퀴
- [ ] `/export` 내보내기 → 파일이 실제로 열리는지
- [ ] 참가자 교체 시 localStorage 초기화 절차
