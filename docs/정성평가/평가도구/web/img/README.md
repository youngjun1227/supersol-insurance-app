# 블록 1 스크린샷 2장

| 파일 | 내용 |
|---|---|
| `home-existing.png` | 기존 슈퍼SOL **보유자** 보험 탭 |
| `home-ours.png` | 우리 화면 상태 B (`?state=B`) |

두 파일이 없으면 설문 화면에 "이미지 준비 중" 자리표시 상자가 뜬다 — 배포는 되지만
블록 1 비교가 성립하지 않으므로 **9/11 전에 반드시 채운다.**

조건 (`../README.md` 와 동일):

- **같은 폰 프레임 · 같은 크기 · 브라우저 UI 없이** — 하나라도 다르면 블라인드가 깨진다
- **기존 화면의 실명·증권번호는 마스킹** — 저장소가 public 이다. 이름은 "김신한"으로 덮는다
- **보험 건수·보험료를 역할과 맞춘다** — 역할은 "보험 2개 · 월 3만 5천 원"
