node_modules/
dist/
.DS_Store
*.local
.env
.env.*
.vercel
.playwright-mcp/
*.png.tmp

# 검수용 임시 스크린샷 (커밋하지 않는다)
/*.png

# 9/11 정성평가 참가자 응답 원본 — public 저장소라 실명·음성이 올라가면 안 된다.
# README 만 추적하고 내용물은 로컬 + 팀 드라이브에 둔다.
docs/정성평가/응답원본/*
!docs/정성평가/응답원본/README.md

# 참고자료의 외부 리포트·자료 원본 — 저작권이 우리에게 없다.
# 한국리서치 리포트는 "무단 복제·전송 금지" 명시. public 저장소라 커밋하면 재배포다.
docs/정성평가/참고자료/*
!docs/정성평가/참고자료/README.md

# 정성평가 스크린샷 원본 — 실명·계정 정보가 있어 커밋 금지. 마스킹한 결과물만 img/ 에 둔다
docs/정성평가/평가도구/web/img/raw/

# Playwright 실행 산출물 — 기준선(e2e/__snapshots__)은 커밋한다, 이건 안 한다
test-results/
playwright-report/
