/* 시연 시나리오 — 진행자가 /demo 에서 고른다.

   왜 있나: 아이디어 3(결제 감지 청구 알림)은 "앱 밖에서 알림을 받고 들어온다"가
   핵심인데, 앱 안 팝업(S4-A)부터 시작하면 그 맥락이 통째로 빠진다.
   백엔드가 없어 진짜 푸시는 못 보내므로 **연출로 재현**한다.

   ⚠️ 실제 알림이 아니다 — 참가자에게는 진행자가 "알림이 왔다고 가정하고
      눌러 보세요"라고 안내한다 (없는 기능을 있다고 믿게 하면 측정이 오염된다). */

export interface Scenario {
  id: string
  /** 진행자 화면에 뜨는 이름 */
  label: string
  /** 이 시나리오가 무엇을 보여 주는지 — 진행자용 한 줄 */
  desc: string
  /** 시작 경로 */
  path: string
}

export const SCENARIOS: Scenario[] = [
  {
    id: 'push-claim',
    label: '카드 결제 → 청구 알림',
    desc: 'iOS 홈화면에서 대기하다 배너가 내려온다. 탭하면 앱이 열리고 청구 팝업까지.',
    path: '/demo/push',
  },
]
