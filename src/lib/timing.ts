/* 시연 연출 타이밍 — 한 곳에 모은다.
   앱 진입(App)과 시연 도입부(DemoPush)가 같은 스플래시를 쓰므로
   값이 갈리면 두 경로의 체감이 달라진다. */

/** 스플래시 유지 시간.
    ⚠️ 디자인 레포 규정은 0.8초(438:10131 AFTER_TIMEOUT)인데 실제로 돌려 보니
       너무 빨라 "앱이 켜졌다"는 느낌이 안 났다. 1.2초로 늘렸다. */
export const SPLASH_MS = 1200

/** 시연 도입부에서 배너가 내려오기까지 — 참가자가 "폰을 보고 있는" 시간 */
export const BANNER_DELAY_MS = 2500
